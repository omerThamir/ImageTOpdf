<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#182430"
    android:orientation="vertical"
    tools:context=".MainActivity">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:padding="5dp"
        android:text=" convert to pdf "
        android:textColor="#fff"
        android:textSize="20dp">

    </TextView>

    <Button
        android:id="@+id/CreateOnePageBtn"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="center"
        android:layout_margin="5dp"
        android:background="@drawable/rounded_changed_background"
        android:text="One image Page"
        android:textColor="#DBDDDF"></Button>

    <Button
        android:id="@+id/Create2imagePerPageBtn"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="center"
        android:layout_margin="5dp"
        android:background="@drawable/rounded_changed_background"
        android:text="Tow Image Page"
        android:textColor="#DBDDDF"></Button>

    <Button
        android:id="@+id/CreatemultiPagesBtn"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="center"
        android:layout_margin="5dp"
        android:background="@drawable/rounded_changed_background"

        android:text="4 image per page"
        android:textColor="#DBDDDF"></Button>


</LinearLayout>